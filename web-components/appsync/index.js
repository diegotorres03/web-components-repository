const AppSyncMutation = require('./appsync-mutation.component')
const AppSyncSubscription = require('./appsync-subscription.component')
const AppSync = require('./appsync.component')

module.exports = {
    AppSync,
    AppSyncMutation,
    AppSyncSubscription,
}